import React from "react"
import "../Styles/Header.css"

function Header(props) {
    return (
        <header>
            <div className={"title"}>
                todo
            </div>
        </header>
    );
}

export default Header;